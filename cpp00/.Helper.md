# 📚 RÉVISION SOUTENANCE - CPP Module 00

## 🎯 CONCEPTS CLÉS À MAÎTRISER

### 1. Différences C vs C++
- **Entrées/Sorties** : `std::cin` / `std::cout` au lieu de `scanf` / `printf`
- **Strings** : `std::string` au lieu de `char*`
- **Allocation** : `new` / `delete` au lieu de `malloc` / `free` (mais pas utilisé ici)
- **Namespaces** : `std::` pour accéder à la bibliothèque standard

### 2. Classes et Objets
**Classe** = blueprint (plan) pour créer des objets
**Objet** = instance d'une classe

```cpp
class PhoneBook {
    private:  // Accessible uniquement dans la classe
        Contact _contacts[8];
        int _index;
    public:   // Accessible de partout
        void addContact();
};
```

---

## 📝 EX00 - MEGAPHONE

### Ce que fait le code :
Convertit tous les arguments en majuscules et les affiche sur UNE SEULE ligne.

### Points clés :
```cpp
std::string x = argv[i];           // Conversion char* → string
x[j] = toupper(x[j]);              // Conversion en majuscule
std::cout << x;                     // Affichage SANS endl (important!)
```

### Questions possibles :
**Q: Pourquoi `toupper()` ?**
R: Fonction C standard qui convertit un caractère en majuscule.

**Q: Pourquoi pas `std::endl` dans la boucle ?**
R: On veut tout sur UNE ligne. `std::endl` est seulement à la fin.

**Q: Que fait `argc` / `argv` ?**
R: `argc` = nombre d'arguments, `argv` = tableau des arguments (strings).

---

## 📞 EX01 - PHONEBOOK

### Architecture :
- **PhoneBook** : Contient 8 contacts max, gère ADD/SEARCH
- **Contact** : Stocke les infos d'une personne (prénom, nom, etc.)

### Fonction `truncate()` :
```cpp
std::string truncate(const std::string &str) {
  if (str.length() > 10)
    return (str.substr(0, 9) + ".");  // 9 caractères + '.'
  else
    return (str);
}
```
**Pourquoi ?** Le sujet demande 10 caractères max, avec '.' si tronqué.

### Fonction `addContact()` :
```cpp
_contacts[_index % 8] = new_contact;  // Modulo 8 pour boucler
_index++;
```
**Pourquoi `% 8` ?** Si on a déjà 8 contacts, on écrase le plus ancien (index 0, puis 1, etc.).

### Fonction `searchContact()` - IMPORTANT :
```cpp
std::cout << std::setw(10) << i << "|";  // Colonne de 10 caractères
```
**`std::setw(10)`** : Définit la largeur à 10 caractères (aligné à droite par défaut).

```cpp
index = atoi(input.c_str());  // Conversion string → int
```
**`atoi()`** : Convertit une string C en entier.
**`.c_str()`** : Convertit `std::string` en `char*` (requis par `atoi`).

### Questions possibles :
**Q: Pourquoi `_index` privé ?**
R: Encapsulation. On ne veut pas que l'utilisateur modifie directement l'index.

**Q: Que fait le constructeur `PhoneBook::PhoneBook() : _index(0)` ?**
R: Liste d'initialisation. Initialise `_index` à 0 avant l'exécution du corps du constructeur.

**Q: Pourquoi vérifier `!std::getline(std::cin, input)` ?**
R: Détecte EOF (Ctrl+D) ou erreur de lecture. Évite les bugs.

**Q: Différence entre `std::endl` et `"\n"` ?**
R: `std::endl` flush le buffer (force l'affichage immédiat), `"\n"` non.

---

## 💰 EX02 - ACCOUNT

### Variables statiques :
```cpp
int Account::_nbAccounts = 0;      // Partagée par TOUS les objets Account
```
**Pourquoi statique ?** On veut compter le nombre TOTAL de comptes, pas par instance.

### Constructeur :
```cpp
Account::Account(int initial_deposit)
{
	_accountIndex = _nbAccounts++;  // Chaque compte a un index unique
	_amount = initial_deposit;
	_totalAmount += initial_deposit; // Met à jour le total global
	// ...
}
```

### Fonction `_displayTimestamp()` :
```cpp
std::time_t t = std::time(NULL);        // Temps actuel (secondes depuis 1970)
std::tm* now = std::localtime(&t);      // Conversion en date/heure locale
std::cout << std::setfill('0') << std::setw(2) << (now->tm_mon + 1);
```
**`std::setfill('0')`** : Remplit avec des '0' (ex: `01` au lieu de `1`).
**`std::setw(2)`** : Largeur de 2 caractères.
**`tm_mon + 1`** : Les mois vont de 0 à 11, donc on ajoute 1.

### Fonction `makeWithdrawal()` :
```cpp
if (withdrawal > _amount) {
	std::cout << "refused" << std::endl;
	return false;  // Retrait refusé
}
// Sinon, on effectue le retrait
_amount -= withdrawal;
```
**Pourquoi vérifier ?** On ne peut pas retirer plus que le solde disponible.

### Questions possibles :
**Q: Pourquoi `_displayTimestamp()` est privée ?**
R: Fonction utilitaire interne, pas besoin de l'exposer publiquement.

**Q: Différence entre variable membre et variable statique ?**
R: Membre = une copie par objet. Statique = UNE SEULE copie partagée.

**Q: Que fait `const` dans `void displayStatus(void) const` ?**
R: Indique que la fonction ne modifie PAS l'objet. Bonne pratique.

---

## 🔥 QUESTIONS PIÈGES CLASSIQUES

### "Pourquoi pas `using namespace std;` ?"
**R:** Le sujet l'INTERDIT explicitement. Sinon grade = -42.

### "C'est quoi Orthodox Canonical Form ?"
**R:** Pas requis pour Module 00 (seulement à partir de Module 02). Mais c'est : constructeur par défaut, constructeur de copie, opérateur d'affectation, destructeur.

### "Pourquoi pas de `malloc` / `free` ?"
**R:** Interdit en C++. On utilise `new` / `delete` (mais pas nécessaire ici, on utilise des tableaux statiques).

### "Différence entre `.h` et `.hpp` ?"
**R:** `.hpp` = convention C++ (indique que c'est du C++, pas du C).

---

## ✅ CHECKLIST AVANT SOUTENANCE

- [ ] Je peux expliquer `std::setw()` et `std::setfill()`
- [ ] Je comprends pourquoi `_index % 8`
- [ ] Je sais ce qu'est une variable statique
- [ ] Je peux expliquer `atoi()` et `.c_str()`
- [ ] Je comprends la différence public/private
- [ ] Je sais pourquoi on vérifie `!std::getline()`
- [ ] Je peux modifier `truncate()` si demandé
- [ ] Je comprends `std::localtime()` et `std::tm`

---

## 🎤 SIMULATION SOUTENANCE

**Évaluateur:** "Explique-moi `_index % 8`."
**Vous:** "Le modulo 8 permet de boucler sur les 8 emplacements. Si j'ai déjà 8 contacts et que j'en ajoute un 9ème, `_index` vaut 8, donc `8 % 8 = 0`, j'écrase le contact à l'index 0."

**Évaluateur:** "Pourquoi `std::setw(10)` ?"
**Vous:** "Le sujet demande des colonnes de 10 caractères. `setw(10)` définit la largeur à 10, aligné à droite par défaut."

**Évaluateur:** "Change la largeur des colonnes à 15."
**Vous:** *Modifie `std::setw(10)` en `std::setw(15)` et change aussi `truncate()` pour 14 + '.'*

**Évaluateur:** "Pourquoi `_totalAmount` est statique ?"
**Vous:** "Parce qu'on veut le montant TOTAL de tous les comptes, pas juste d'un seul. Une variable statique est partagée par toutes les instances."

---

## 💡 DERNIERS CONSEILS

1. **Restez calme** : Vous avez écrit ce code, vous le comprenez.
2. **Testez avant** : Lancez vos programmes, vérifiez qu'ils compilent.
3. **Relisez le sujet** : Vérifiez que vous respectez TOUS les points.
4. **Soyez honnête** : Si vous ne savez pas, dites "Je ne suis pas sûr, mais je pense que...".

**Bonne chance ! 🚀**
